# Android App for sahilrajput.com

This project is created by:

```bash
# Run on 6 Oct, 2023
bubblewrap init --manifest https://sahilrajput.com/manifest.json

# Logs of above command can be found in file -
# `logs-bubble-wrap-init.txt`
```

## Project details

- APK name: `com.sahilrajput.twa`
- More details can be found @ `file://./app/build.gradle`
- Uses android SDK from `sdk.dir=/home/array/Android/Sdk`. This value is set by default in file `local.properties` in the project directory.
